# 玄机阁 环境变量示例（占位文件，仅用于示范结构）
# 真实配置写入项目外的外部 secrets 文件，不要写进项目目录内任何文件：
#   Windows 默认：C:\Users\<你>\.workbuddy\xuanji-secrets.env
#   Linux/Mac 默认：~/.workbuddy/xuanji-secrets.env
#   （可用环境变量 XUANJI_SECRET_FILE 覆盖此路径）
# 代码（llm.py / auth.py）会自动读取该外部文件；部署时则用 startCmd 注入环境变量。

# ===== 智谱 GLM API Key =====
# 部署时通过 startCmd 注入：ZHIPU_API_KEY=xxx
ZHIPU_API_KEY=your_zhipu_api_key_here

# ===== JWT 签名密钥 =====
# 部署时通过 startCmd 注入：XUANJI_JWT_SECRET=$(python -c 'import secrets;print(secrets.token_hex(32))')
# 用于签发和验证 JWT，绝对不要进版本库。生产环境务必用 32 字节以上的强随机值。
XUANJI_JWT_SECRET=replace-with-secrets-token-hex-32

# ===== 邮件服务（可选）=====
# 配了就能真实发邮件给注册用户；不配就保留"页面直接显示验证码"的开发兜底。
#
# 主流 provider 二选一即可（不需要全部启用）：
#
# ------ QQ 邮箱（推荐之一）------
# 拿授权码步骤：登录 mail.qq.com → 设置 → 账户 → 开启「SMTP 服务」→ 生成授权码（16 字符）
# MAIL_PROVIDER=qq
# MAIL_USER=12345678@qq.com
# MAIL_PASSWORD=abcdefghijklmnop    # 这里贴你刚拿到的 16 位授权码（不是 QQ 密码）
#
# ------ Gmail（需要先开两步验证）------
# 拿 App Password 步骤：myaccount.google.com → 安全性 → 两步验证 → 应用专用密码 → 选"邮件/其它"生成 16 字符
# MAIL_PROVIDER=gmail
# MAIL_USER=yourname@gmail.com
# MAIL_PASSWORD=abcdefghijklmnop    # 这里贴 16 位 App Password（不是 Google 密码）
#
# ------ 网易 163 / 126 ------
# 拿授权码步骤：登录 mail.163.com → 设置 → POP3/SMTP/IMAP → 开启 SMTP 服务 + 客户端授权码
# MAIL_PROVIDER=163
# MAIL_USER=yourname@163.com
# MAIL_PASSWORD=授权码
#
# ------ 完全自定义（高级：自有域名邮箱、企业邮）------
# MAIL_PROVIDER=custom
# MAIL_HOST=smtp.yourdomain.com
# MAIL_PORT=465
# MAIL_SSL=1
# MAIL_USER=noreply@yourdomain.com
# MAIL_PASSWORD=your_smtp_password
#
# ------ 可选 overrides ------
# MAIL_FROM=noreply@yourdomain.com    # 默认 = MAIL_USER；QQ/163 严格要求与登录账户一致
# MAIL_PORT=465                       # 覆盖预设的端口
# MAIL_SSL=1                          # 覆盖预设的 SSL/STARTTLS（1=SSL, 0=STARTTLS）

# 部署方式（用 workbuddy_sites_deploy 时）：
# startCmd="MAIL_PROVIDER=qq \
#           MAIL_USER='12345678@qq.com' \
#           MAIL_PASSWORD='abcdefghijklmnop' \
#           ZHIPU_API_KEY=xxx \
#           XUANJI_JWT_SECRET=xxx \
#           python server.py"

# 安全提醒：
# - 这些是邮件凭据 = 跟你的 QQ/Google 账号同权。一旦泄露，对方能用你的邮箱发邮件。
# - 永远不要把真实 .env 文件提交到 git。.gitignore 已排除 .env / .env.* / *.env。
# - 部署时环境变量是注入到容器里运行的，不会进部署包源码，也不会写盘。
